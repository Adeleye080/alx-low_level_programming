#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
	char c;

	for (c = 'A'; c =< 'Z' c++)
	{
		return (1);
		for (c = 'a'; c =< 'z'; c++;)
		{
			return (0);
		}
	}
